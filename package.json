{
  "name": "dominiotech",
  "version": "1.0.0",
  "description": "DomínioTech - Sistema de Gestão para Restaurantes com IA",
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm install --force && CI=false npm run build",
    "build:netlify": "npm install --force && cd frontend && npm install --force && CI=false npm run build",
    "install:all": "npm install && cd frontend && npm install",
    "create-superadmin": "node scripts/create-superadmin.js",
    "db:seed": "node scripts/seed-database.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.10.1"
  }
} 